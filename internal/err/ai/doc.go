// Package ai provides constructors for ctx ai command errors.
//
// It centralizes proposal validation failures so command code can reuse
// them and satisfy repository audit rules without naked errors.
// The package is intentionally narrow and only covers ctx ai surfaces.
package ai
