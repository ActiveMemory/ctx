# Architecture Enrichment Refresh (2026-06)

## Problem

The graph-verified architecture artifacts in `.context/` dated
from the 2026-04-03 enrichment pass (index bf42b1f6, 19.3k
symbols). The codebase has since grown to 27.9k symbols — new
subsystems (hub, steering, trigger, kb, connection, desktop) and
8 new CLI command families — so blast-radius numbers, extension
inventories, and hotspot tables were materially stale.

## Design

Re-run `/ctx-architecture-enrich` against a fresh GitNexus index
(@ 60d8e823) and update the enrichment layer in place,
idempotently replacing the 2026-04-03 annotations:

- **DANGER-ZONES.md**: re-verified 9 symbols. desc.Text d=1
  940 (was "30+"), SafeWriteFile 95 (was 69), rc.RC new
  CRITICAL entry (d=1 34, 8 modules), DiscoverPath downgraded
  to HIGH (single domain). Three explicit undercount flags
  where the graph disagrees with read knowledge.
- **EXTENSION-POINTS.md**: registration inventories refreshed
  with file:line refs — 42 CLI commands, 15 MCP tools, 12 drift
  checks, 8 setup deployers.
- **CHEAT-SHEETS.md**: flow index and multi-flow hotspot table
  rebuilt; hook preamble pair (40/39 flows) newly surfaced.
- **ARCHITECTURE-PRINCIPAL.md**: clustering comparison updated;
  key finding is coverage gaps (new subsystems with no manual
  baseline), not boundary mismatches.
- **DETAILED_DESIGN-{foundation,cli}.md + map-tracking.json**:
  validate and compliance enriched to 0.75 with call-graph and
  source evidence.
- **CLAUDE.md / AGENTS.md**: GitNexus-managed sections
  regenerated by `npx gitnexus analyze` (symbol counts, skill
  tables).

## Follow-up

The enrichment flagged that the manual baseline is outgrown:
a `/ctx-architecture` refresh pass should precede the next
enrichment (noted in CONVERGENCE-REPORT.md).
